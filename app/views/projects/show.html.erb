<h1><%= @project.body %></h1>

<h2>Things:</h2>

<div id="droparea">
  Drop a file here.
</div>

<% @project.things.map.each { |thing| %>
    <p><img src="<%= thing.url %>"></p>
    <p><%= thing.inspect %></p>    
<% } %>

<%= link_to 'Edit', edit_project_path(@project) %> |
<%= link_to 'Back', projects_path %>

<script>
var page_entity = <%== @project.to_json %>;
</script>
